services:
  web:
    build: ../app
    command: uv sync && python manage.py runserver
    volumes:
      - ./app/:/usr/src/app/
    ports:
      - 8000:8000
    env_file:
      - ./.env.dev

networks:
  default:
    driver: bridge
    name: private_frontend
    external: true